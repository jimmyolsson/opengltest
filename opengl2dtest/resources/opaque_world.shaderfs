#version 430 core
out vec4 FragColor;

in vec3 FragPos;
in vec2 TexCoords;
in float LightValue;
in float BType;

uniform sampler2DArray texture1;

void main()
{    
	if(texture(texture1, vec3(TexCoords.xy, BType)).a < 0.1)
		discard;
	FragColor = texture(texture1, vec3(TexCoords.xy, BType));
}